import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ecolink.app',
  appName: 'ecolink',
  webDir: 'dist/ecolink/browser'
};

export default config;
